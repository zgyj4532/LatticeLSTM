[project]
name = "lattice-lstm"
version = "0.1.0"
description = "Chinese NER using Lattice LSTM"
requires-python = ">=3.11,<3.12"
dependencies = [
    "numpy>=1.26",
    "torch>=2.2,<3",
]

[tool.pyright]
pythonVersion = "3.11"
pythonPlatform = "Windows"
venvPath = "."
venv = ".venv"
